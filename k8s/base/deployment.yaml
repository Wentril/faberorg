apiVersion: apps/v1
kind: Deployment
metadata:
  name: faberorg-web
  namespace: faberorg
spec:
  replicas: 1
  selector:
    matchLabels:
      app: faberorg-web
  template:
    metadata:
      labels:
        app: faberorg-web
    spec:
      containers:
      - name: web
        image: your-registry/faberorg:latest # TODO
        ports:
        - containerPort: 8000
        envFrom:
        - secretRef:
            name: faberorg-secret
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
